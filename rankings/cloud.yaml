# Cloud model rankings — external public benchmarks only
# No live benchmark calls (quota protection policy)
# Sources:
#   arena_elo — lmmarketcap.com/benchmarks/arena_elo (2026-06-29)
#   groq scores — console.groq.com model pages (2026-06)
#   openrouter — openrouter.ai/models (2026-06-29)
# slot 0 = best for category; cascade order: NVIDIA NIM → Groq → OpenRouter
#
# Rate limits (free tier):
#   NVIDIA NIM:   40 RPM global, 1 000 credits on signup — build.nvidia.com
#   Groq:         30 RPM / 1 000 RPD / 8 K TPM per model
#   OpenRouter:   20 RPM / 200 RPD per model
#
# Auth env vars: NVIDIA_API_KEY, GROQ_API_KEY, OPENROUTER_API_KEY
# NOTE: Groq llama-3.3-70b-versatile deprecated 2026-06-29, decommissioned 2026-08-16
# NOTE: z-ai/glm-5.2 added 2026-07-19 — successor to glm-5.1 (EOL 2026-07-02),
#   free on NVIDIA NIM since 2026-07-02. 753B MoE, 1M context, top open-weight
#   coding model (SWE-bench Pro 62.1, Terminal-Bench 2.1 81.0). MIT license.
#   Source: docs.api.nvidia.com/nim/reference/z-ai-glm-5.2 — 2026-07
# NOTE 2026-07-21: OpenRouter free-tier catalog rotated — verified live against
#   GET https://openrouter.ai/api/v1/models (no key required, filtered to
#   id.endswith(':free')). Three slugs used below had gone paid-only and were
#   404ing every cascade call: qwen/qwen3-next-80b-a3b-instruct:free,
#   openai/gpt-oss-120b:free, meta-llama/llama-3.3-70b-instruct:free. Replaced
#   with currently-free slugs from that same live listing (google/gemma-4-*:free
#   and nvidia/nemotron-3-super-120b-a12b:free were re-checked and are still
#   live, unchanged). New slots have no arena_elo — these models aren't on
#   lmmarketcap yet — so they carry a plain descriptive note instead of a
#   fabricated score.
# WATCH: Kimi K3 (2.8T, released 2026-07-16, #3 on Artificial Analysis) is
#   paid-only so far (OpenRouter moonshotai/kimi-k3, $3/$15 per Mtok). Weights
#   due ~2026-07-27 — recheck build.nvidia.com then for a free NIM listing.
#   Microsoft MAI-Thinking-1/MAI-Code-1 (Build 2026) have no free tier and are
#   not on NVIDIA NIM — skip until a free/cheap provider picks them up.
# EVALUATED 2026-07-23 (OpenRouter "task-aware routing" email, mid-July 2026
#   changelog) — none of these fit the free-tier cascade policy, not added:
#   - openrouter/auto-beta: meta-router, $0 routing fee but bills at the
#     underlying model's rate (i.e. not itself free) and picks the model
#     black-box per request. Conflicts with this project's design (we own
#     the ranking/cascade logic in this file); skip.
#   - moonshotai/kimi-k3: confirmed paid-only, $3/$15 per Mtok, no free
#     hosted route yet. Same conclusion as the note above — still watching
#     for a free NIM listing after weights drop.
#   - kwaipilot/kat-coder-air-v2.5 (and -pro-v2.5): cheap but not free —
#     $0.15/$0.60 per Mtok, 256K context. Good agentic-coding scores but no
#     ":free" OpenRouter slug exists; recheck if one appears.
# NOTE 2026-07-23: OpenRouter free catalog re-scanned live (curl .../api/v1/models,
#   filtered ':free', 15 hits). Two new adds: poolside/laguna-s-2.1:free (coding,
#   coding/code_debug — 118B-A8B, SWE-Bench Multilingual 78.5, strong agentic
#   coding for its size) and inclusionai/ling-3.0-flash:free (reasoning/
#   instruction — 124B-A5.1B, ~Sonnet-4.6 class). Ling-3.0-flash is a TIME-
#   LIMITED launch promo, free only through 2026-08-03 — recheck
#   openrouter.ai/api/v1/models around then and drop/replace it if it goes paid.
#   Also live but not added (off-category or redundant): poolside/laguna-xs-2.1
#   and laguna-m.1 (smaller Laguna variants, no clear category gap yet),
#   nvidia/nemotron-3.5-content-safety:free (classifier, not general chat),
#   nvidia/nemotron-nano-12b-v2-vl:free (vision-language, no vision category
#   here), nvidia/nemotron-nano-9b-v2:free (small/fast, no speed-ranked
#   category besides multilingual which is already Groq-first).
# NOTE 2026-08-03: Live-verified against GET integrate.api.nvidia.com/v1/models
#   and per-model chat completion calls. Two entries were silently 410ing in
#   production: qwen/qwen3.5-397b-a17b and minimaxai/minimax-m2.7 both hit
#   EOL 2026-07-27 ("has reached its end of life ... no longer available").
#   Removed from coding, instruction, multilingual, context. Two new free
#   NVIDIA NIM models added: deepseek-ai/deepseek-v4-flash (Terminal-Bench 2.1
#   82.7, beats v4-pro's 72.1 and glm-5.2's 81.0 on DeepSeek's own agent/coding
#   suite, 13B active/1M context — techtimes.com/articles/322513, verified
#   live 2026-08-03) replaces deepseek-v4-pro in coding/code_debug (kept in
#   reasoning/math/summarization/context, untested there); and
#   thinkingmachines/inkling (975B MoE/41B active, Apache 2.0, SWE-bench 77.6,
#   #12 in Instruction Following per public leaderboard — thinkingmachines.ai/
#   model-card/inkling, verified live 2026-08-03) added to reasoning and
#   instruction. Evaluated but not added: stepfun-ai/step-3.7-flash (198B MoE,
#   SWE-Bench Pro 56.3 — weaker than glm-5.2/deepseek-v4-flash already in
#   coding); nvidia/nemotron-nano-3-30b-a3b (listed in /v1/models but 404s on
#   chat completions — not yet live).
# NOTE 2026-08-12: Meta released Muse Glimmer 2026-08-10 — first open-weight
#   agentic model from Meta Superintelligence Labs (Apache 2.0), distilled
#   from Muse Spark 1.2. 30B dense, 131K context, free on NVIDIA NIM
#   (meta/muse-glimmer-30b — docs.api.nvidia.com/nim/reference/meta-muse-glimmer-30b,
#   build.nvidia.com/meta/muse-glimmer-30b). Scores: AA-LCR (long-context
#   reasoning) 80.0 vs Gemma4-31b's 68.3 and Qwen3.6-27b's 73.3; reasoning
#   +5 over Gemma4-31b; MCP-Atlas (agentic/tool-use) 75.5 vs Qwen3.6-27b's
#   62.5 and Gemma4-31b's 54.2 — matches 1T-param Kimi K2.5 with 33x fewer
#   params. Added to reasoning, instruction, coding, code_debug (agentic
#   tool-use score is the strongest fit there). Not added to context — 131K
#   is far below the 1M-context entries already covering that category.
#   Source: developer.nvidia.com/blog/run-local-agentic-ai-workflows-with-metas-muse-glimmer-on-nvidia,
#   artificialanalysis.ai/articles/muse-glimmer — verified 2026-08-12.
# NOTE 2026-08-12 (urgent fixes, live-verified against console.groq.com and
#   openrouter.ai/api/v1/models):
#   1. Groq deprecates llama-3.1-8b-instant AND llama-3.3-70b-versatile on
#      2026-08-16 (announced 2026-06-17, console.groq.com/docs/deprecations).
#      llama-3.1-8b-instant was the Groq fast/last-resort entry in 8 of 9
#      categories — replaced everywhere with Groq's official migration
#      target openai/gpt-oss-20b, EXCEPT in `instruction`, which swapped to
#      llama/llama-4-scout-17b-16e-instruct instead: gpt-oss-20b shares the
#      gpt-oss-120b reasoning family this category already excludes for
#      <think>-block token burn on low-max_tokens calls; Scout is a
#      non-reasoning Llama 4 MoE instruct model and doesn't have that problem.
#      llama-3.3-70b-versatile is not used anywhere in this file — no action.
#   2. inclusionai/ling-3.0-flash:free's time-limited promo (flagged in the
#      2026-07-23 note above) ended 2026-08-03 as warned; confirmed gone from
#      the live OpenRouter free catalog. Removed from `reasoning` (replaced
#      with nvidia/nemotron-3.5-lightning:free, new 2026-08-11, 30B MoE/3B
#      active, fast agentic — blogs.nvidia.com/blog/nemotron-lightning-
#      switchyard-rtx-dgx) and from `instruction` (removed, not replaced —
#      Lightning's reasoning-trace behavior isn't yet verified and this
#      category can't risk another <think>-block regression).
#   Live-checked and still fine, no action needed: all other OpenRouter
#   :free slugs used in this file (gemma-4-31b/26b-it, gpt-oss-20b,
#   nemotron-3-{nano-30b-a3b,nano-omni-30b-a3b-reasoning,super-120b-a12b,
#   ultra-550b-a55b,nano-9b-v2}, poolside/laguna-s-2.1, cohere/north-mini-code)
#   — all 16 currently live. NVIDIA NIM models (glm-5.2, deepseek-v4-flash/
#   pro, kimi-k2.6, minimax-m3, inkling, muse-glimmer-30b) — no EOLs found.
#   Kimi K3 weights are open (custom license) but still no free NIM/Groq
#   listing; MAI-Thinking-1/MAI-Code-1 still enterprise-only. Both remain
#   correctly un-added per existing WATCH note.
# NOTE 2026-07-27: Added `cheapest:<category>` and `fastest:<category>` as
#   model-string modes alongside `best:<category>` (see router/registry.py).
#   `cheapest` keeps free-tier entries first (in curated order) then appends
#   any `paid: true` entries sorted ascending by blended $/Mtok — free entries
#   are always tried first, paid ones are a last resort. `fastest` reorders
#   free-tier entries only by provider speed (Groq > NVIDIA NIM > OpenRouter),
#   with per-model overrides where we have documented real numbers. Paid
#   entries are excluded from `best:`/hybrid cascades automatically via the
#   `paid: true` flag — they only ever surface through `cheapest:`.
#   This is what finally puts the two paid coding models evaluated above to
#   use: kwaipilot/kat-coder-air-v2.5 ($0.15/$0.60, cheapest paid rung) and
#   moonshotai/kimi-k3 ($3/$15, priciest/last rung) are now in `coding` and
#   `code_debug` as `paid: true` fallback entries — reachable only via
#   `cheapest:coding` / `cheapest:code_debug` once every free option fails.
# NOTE 2026-08-22 (model-scout daily audit): live probes against
#   integrate.api.nvidia.com/v1/chat/completions (not just /v1/models listing —
#   catches models still listed but dead) found two NEW EOLs since the last
#   audit: z-ai/glm-5.2 on NVIDIA NIM (EOL 2026-08-21, used in coding/
#   code_debug/context) and deepseek-ai/deepseek-v4-flash on NVIDIA NIM (EOL
#   2026-08-07, used in coding/code_debug) — the latter's dated build id
#   deepseek-ai/deepseek-v4-flash-0731 is still live and is what these entries
#   now point to. glm-5.2 itself is NOT dead everywhere — it's still free on
#   OpenRouter (z-ai/glm-5.2:free) — only the NVIDIA NIM listing died.
#   Replacement for glm-5.2's NVIDIA slot in coding/code_debug/context:
#   moonshotai/kimi-k3 (2.8T MoE, #3 Artificial Analysis 2026-07-16), whose
#   weights went open 2026-07-26. It was still paid-only on OpenRouter as of
#   the 2026-07-27 audit, but a live probe today confirms NVIDIA NIM now
#   serves it for free (200 OK) — this is NOT yet documented in NIM's public
#   model catalog or docs.api.nvidia.com, verified only by direct API call.
#   Recheck in a future audit that this holds and isn't a temporary/unlisted
#   state. Since kimi-k3 is now free via NIM, the `paid: true` OpenRouter
#   kimi-k3 fallback entries (added 2026-07-27 in coding/code_debug) are
#   removed as redundant — kwaipilot/kat-coder-air-v2.5 remains the sole
#   `cheapest:*` paid rung.
#   Cross-checked kimi-k3 against both required ranking sources (per updated
#   model-scout policy: LMArena direct as primary, Artificial Analysis as
#   independent backup, not just lmmarketcap aggregation) — LMArena added it
#   to Text/Code/Agent Arena leaderboards 2026-07 (#1 Frontend Code Arena);
#   Artificial Analysis Intelligence Index v4.1.1 (2026-08-20) scores it 60,
#   tied top open-weight model with GLM-5.3, behind only Opus 5/Fable 5/
#   GPT-5.6 Sol. Both sources agree it's a legitimate top-tier upgrade over
#   glm-5.2, not just a self-reported number.
#   New provider candidates found via open web search (not yet integrated —
#   each needs a new API key, left as a proposal in plans/new-cloud-providers.md
#   rather than added blindly): Cerebras (free tier, ~1M tokens/day, no card,
#   LPU-speed like Groq) and Google Gemini API (free tier, large context, no
#   card). Both worth a follow-up integration pass.
#   Local model check (Ollama, this machine's 16GB RAM / Apple Silicon
#   constraint): no upgrade found over the currently pinned qwen3.5:9b
#   daily-driver (installed 2026-08-12). General "best local LLM 2026" search
#   results were SEO-blog aggregators citing stale picks (Llama 3.1 8B, Phi-4,
#   Qwen 3 7B) already superseded by what's installed — not actionable,
#   discarded per this project's policy against trusting blog specifics.
# NOTE 2026-08-24 (model-scout daily audit): live probes (direct chat-completion
#   calls, not just /v1/models listings) found three EOLs since the last audit:
#   1. deepseek-ai/deepseek-v4-pro on NVIDIA NIM — HTTP 410 "reached its end of
#      life on 2026-08-07T09:00:00Z". Was used in reasoning/math/context.
#      Replaced everywhere with deepseek-ai/deepseek-v4-flash-0731 (already
#      live in coding/code_debug/context since the 2026-08-22 audit) — same
#      successor model, now extended to the remaining categories.
#   2. openai/gpt-oss-20b:free on OpenRouter — HTTP 404, "unavailable for
#      free, use openai/gpt-oss-20b [paid] instead". Was used as the
#      OpenRouter fallback in reasoning/coding/math/summarization/instruction/
#      code_debug. In reasoning and instruction, replaced with
#      thinkingmachines/inkling:free — newly free on OpenRouter as of this
#      audit (previously only free via NVIDIA NIM), and already vetted for
#      both categories (inkling has no <think>-block token-burn issue, it's
#      already used in `instruction` via NIM). In coding/math/summarization/
#      code_debug, removed with no replacement — each of those categories
#      still has 3-4 other live OpenRouter free entries, no coverage gap.
#   3. llama/llama-4-scout-17b-16e-instruct on Groq — HTTP 404. Confirmed via
#      GET api.groq.com/openai/v1/models: fully gone from Groq's catalog, not
#      just renamed (checked meta-llama/ prefix too — not present). Groq's
#      free catalog is now just openai/gpt-oss-{20b,120b}, qwen/qwen3.6-27b
#      (all reasoning-family, excluded from `instruction` by this category's
#      own long-standing rule), plus allam-2-7b (Arabic-specialized) and
#      groq/compound(-mini) (agentic tool-use systems, not plain instruct).
#      GAP: no non-reasoning general instruct model currently available on
#      Groq at all. Removed the Groq entry from `instruction` with no
#      replacement — category still covered by NVIDIA (inkling, muse-
#      glimmer-30b) and OpenRouter (gemma-4-31b, inkling:free) entries, but
#      the "fast Groq LPU" option for this category is gone until Groq adds
#      a new non-reasoning instruct model. Recheck in a future audit.
#   Live-checked and confirmed still fine: openai/gpt-oss-20b (Groq, paid-tier
#   name but free on Groq), openai/gpt-oss-120b (Groq), qwen/qwen3.6-27b
#   (Groq), moonshotai/kimi-k3, deepseek-ai/deepseek-v4-flash-0731, meta/
#   muse-glimmer-30b, moonshotai/kimi-k2.6, minimaxai/minimax-m3,
#   thinkingmachines/inkling (all NVIDIA NIM — all 200 OK). New OpenRouter
#   free-tier additions spotted but not integrated (off-category or
#   redundant with existing entries): dots-studio/dots-3-note-preview:free,
#   liquid/lfm-2.5-2.6b:free (small/fast, no clear category gap),
#   thinkingmachines/inkling-small:free, poolside/laguna-xs-2.1:free,
#   z-ai/glm-5.2:free (still alive on OpenRouter free tier, unchanged from
#   2026-08-22 note). No new free cloud providers or local-model upgrades
#   found this pass.
# NOTE 2026-08-24 (new provider — Pulpo/Luganense incident): Luganense's
#   classifier cascade went fully down today when Groq/NVIDIA/OpenRouter all
#   failed at once (Groq quota exhausted, one NVIDIA model dead, OpenRouter
#   rate-limited) — a single-incident, all-tiers-down scenario. Added
#   `gemini` as a 4th independent free provider (own quota, own infra) to
#   `multilingual`, `instruction`, and `context` — the categories that most
#   directly cover Luganense's classifier and general instruct use. No card
#   required for Gemini's free tier. Evaluated and REJECTED `cerebras` for
#   the same purpose: its no-card free tier ended, now requires a payment
#   method for a $5/30-day credit — breaks this project's no-card cascade
#   policy, not added.
# NOTE 2026-08-27 (model-scout daily audit): live probes (direct chat-completion
#   calls against every model referenced in this file, not just listing
#   endpoints) found four EOLs/breakages since the last audit:
#   1. moonshotai/kimi-k2.6 on NVIDIA NIM — HTTP 404 "Not found for account",
#      consistently reproduced, even though the model id is STILL listed in
#      GET /v1/models. Treated as dead for this account. Was used in
#      reasoning/math/summarization/multilingual (4 categories). Replaced
#      everywhere with moonshotai/kimi-k3 (already live and used in
#      coding/code_debug/context since the 2026-08-22 audit).
#   2. thinkingmachines/inkling on NVIDIA NIM — HTTP 410 "reached its end of
#      life on 2026-08-25T08:00:00Z". Was used in reasoning/instruction.
#      Removed both places, no replacement (muse-glimmer-30b/deepseek-v4-
#      flash-0731 already cover those slots).
#   3. thinkingmachines/inkling:free on OpenRouter — HTTP 403 "only available
#      on agentic harnesses", not usable as a plain chat-completion model
#      (checked its smaller sibling thinkingmachines/inkling-small:free too —
#      same 403). Was used in reasoning/instruction. In `reasoning`, replaced
#      with z-ai/glm-5.2:free (confirmed alive on the live free catalog,
#      though it 429s under upstream congestion — not EOL, kept per policy).
#      In `instruction`, removed with NO replacement — glm-5.2's <think>-
#      block behavior is unverified and this category excludes reasoning
#      traces by design; gemma-4-31b-it:free and Gemini already cover it.
#   4. nvidia/nemotron-3-nano-30b-a3b:free on OpenRouter — HTTP 404
#      "unavailable for free" (paid-only now). Was used in math/
#      summarization. Removed both places, no replacement needed (2+ live
#      free entries remain in each).
#   5. nvidia/nemotron-nano-9b-v2:free on OpenRouter — HTTP 404 "No endpoints
#      found". Was used in context as the smallest fallback. Removed, no
#      replacement needed (3 live entries remain).
#   Live-checked and confirmed still fine (429s are upstream rate-limits, not
#   EOLs, left unchanged): google/gemma-4-31b-it:free, google/gemma-4-26b-
#   a4b-it:free (both 429 on OpenRouter's shared Google AI Studio pool),
#   moonshotai/kimi-k3 (429 on NVIDIA NIM, same pattern as prior audits where
#   it was confirmed live). Also confirmed live: deepseek-ai/deepseek-v4-
#   flash-0731, meta/muse-glimmer-30b, minimaxai/minimax-m3 (all NVIDIA NIM);
#   openai/gpt-oss-120b, qwen/qwen3.6-27b, openai/gpt-oss-20b (all Groq);
#   poolside/laguna-s-2.1:free, cohere/north-mini-code:free, nvidia/nemotron-
#   3-ultra-550b-a55b:free, nvidia/nemotron-3-super-120b-a12b:free, nvidia/
#   nemotron-3.5-lightning:free, nvidia/nemotron-3-nano-omni-30b-a3b-
#   reasoning:free (all OpenRouter); gemini-3.1-flash-lite, gemini-3.5-flash
#   (both Gemini).
#   Also fixed two code-level staleness bugs found while cross-checking this
#   file against the codebase: router/dispatcher.py's _NVIDIA_THINKING_DEFAULTS
#   still keyed off dead deepseek-ai/deepseek-v4-pro and unsuffixed
#   deepseek-ai/deepseek-v4-flash (both EOL since 2026-08-07) and
#   moonshotai/kimi-k2.6 (dead per above) — updated to deepseek-ai/deepseek-
#   v4-flash-0731 and moonshotai/kimi-k3, matching what's actually live and
#   used in this file. benchmark/providers/openrouter_provider.py's
#   FREE_MODELS still listed the two dead OpenRouter entries fixed above
#   (thinkingmachines/inkling:free, nvidia/nemotron-nano-9b-v2:free) —
#   updated to z-ai/glm-5.2:free and nvidia/nemotron-3-super-120b-a12b:free.
#   New OpenRouter free-tier additions spotted but not integrated (off-
#   category, redundant, or same agentic-harness-only restriction as
#   inkling): dots-studio/dots-3-note-preview:free and liquid/lfm-2.5-2.6b:free
#   (both emit a reasoning trace and returned empty content under a 20-token
#   cap — same failure mode already excluded from `instruction`; no clear
#   category gap to justify the risk elsewhere), poolside/laguna-xs-2.1:free
#   (smaller Laguna variant, redundant with laguna-s-2.1 already in use),
#   minimax/minimax-m3:free and minimax/minimax-m2.7:free (redundant with
#   minimaxai/minimax-m3 already covering this on NVIDIA NIM).
#   No new free cloud providers found this pass. Local model check (Ollama,
#   16GB RAM Apple Silicon): no upgrade search performed this pass — deferred,
#   nothing indicated a change since the 2026-08-24 note.
# NOTE 2026-08-28 (model-scout daily audit): live probes (direct chat-completion
#   calls, not just listing endpoints) against every model referenced in this
#   file — NVIDIA NIM (deepseek-ai/deepseek-v4-flash-0731, moonshotai/kimi-k3,
#   meta/muse-glimmer-30b, minimaxai/minimax-m3), Groq (openai/gpt-oss-20b,
#   openai/gpt-oss-120b, qwen/qwen3.6-27b), OpenRouter (nvidia/nemotron-3.5-
#   lightning:free, google/gemma-4-{31b,26b-a4b}-it:free, nvidia/nemotron-3-
#   nano-omni-30b-a3b-reasoning:free, z-ai/glm-5.2:free, nvidia/nemotron-3-
#   {super-120b-a12b,ultra-550b-a55b}:free, poolside/laguna-s-2.1:free,
#   cohere/north-mini-code:free), and Gemini (gemini-3.1-flash-lite,
#   gemini-3.5-flash) — found ZERO EOLs. All 200 OK except four OpenRouter
#   free entries that 429'd (gemma-4-31b-it, gemma-4-26b-a4b-it, glm-5.2,
#   laguna-s-2.1 — all "temporarily rate-limited upstream shared pool", the
#   same recurring congestion pattern from prior audits, not EOL, kept per
#   policy. Re-scanned the live OpenRouter free catalog
#   (GET /v1/api/v1/models, filtered ':free'): one new entry since the last
#   scan, inclusionai/ling-3.0-flash-fin:free (finance-domain fine-tune of the
#   earlier ling-3.0-flash, which had already been removed as a time-limited
#   promo). Probed it directly — works, but emits a reasoning trace before
#   the answer (same <think>-block risk already excluded from `instruction`)
#   and is finance-specialized, off-category for every existing slot. Not
#   added. Also re-scanned the NVIDIA NIM catalog (GET /v1/models): confirmed
#   moonshotai/kimi-k2.6 still 404s ("Not found for account", dead for this
#   account, matches 2026-08-27 finding, no longer referenced anywhere in
#   this file so no action) and nvidia/nemotron-nano-3-30b-a3b still 404s on
#   chat completions despite being listed (still not live). Found a new dated
#   build deepseek-ai/deepseek-v4-pro-0813 (200 OK) — the old unsuffixed
#   deepseek-v4-pro's EOL replacement — but deepseek-v4-flash-0731 (already
#   in this file) beats it on every published benchmark (Terminal-Bench 2.1
#   82.7 vs 72.1), so not swapped in. Web search for new open-weight releases
#   and local (Ollama/Apple Silicon 16GB) upgrades returned only SEO-blog
#   aggregators with no dated primary-source releases — discarded per
#   long-standing project policy, nothing actionable. No changes to any
#   provider file, registry, or ranking this pass.
# NOTE 2026-08-29 (model-scout daily audit): live probes (direct chat-completion
#   calls, not just listing endpoints) against every model referenced in this
#   file. Found ONE real issue: meta/muse-glimmer-30b on NVIDIA NIM hangs
#   indefinitely — TLS/HTTP2 connects fine but no response ever arrives,
#   confirmed across 3 separate probes with max-time up to 240s (not a
#   transient blip). Not a clean 404/410 EOL, so the usual EOL-removal rule
#   doesn't literally apply, but it's functionally worse: it sat as the
#   FIRST entry in `instruction`, so every best:instruction call would burn
#   the full 120s NVIDIA dispatcher timeout before falling back. Removed
#   from all 4 categories it was in (reasoning, coding, code_debug,
#   instruction); see per-category notes. `instruction` now has no NVIDIA
#   entry at all — flagged as a gap there, still covered by OpenRouter
#   (gemma-4-31b-it:free) and Gemini (gemini-3.1-flash-lite).
#   Also fixed a code-level staleness bug: router/dispatcher.py's
#   _NVIDIA_THINKING_DEFAULTS still had two dead keys (qwen/qwen3.5-397b-a17b,
#   qwen/qwen3.5-122b-a10b, both EOL 2026-07-27 per the 2026-08-03 note,
#   already removed from every category and from registry.py's speed
#   overrides on 2026-08-22) — removed, harmless but dead weight.
#   Everything else live-checked and confirmed still fine: deepseek-ai/
#   deepseek-v4-flash-0731 and moonshotai/kimi-k3 (both NVIDIA NIM, 200 OK),
#   minimaxai/minimax-m3 (NVIDIA NIM, 200 OK), openai/gpt-oss-{20b,120b} and
#   qwen/qwen3.6-27b (all Groq, 200 OK), gemini-3.1-flash-lite and
#   gemini-3.5-flash (both Gemini, 200 OK — two isolated 404s on first probe
#   turned out to be a transient network blip, not a real API 404: retried
#   and got clean 200s with valid completions both times). Re-scanned the
#   live OpenRouter free catalog (GET /v1/api/v1/models, filtered ':free'):
#   unchanged from the 2026-08-28 scan, same 18 entries (still includes the
#   already-evaluated-and-rejected inclusionai/ling-3.0-flash-fin:free). Web
#   search for new open-weight releases surfaced Qwen3.8-2.4T-A95B/Qwen3.8-27B
#   (Alibaba, ~2026-08-12/14) and GLM-5.3 / GLM-5.3-Flash (Z.ai, 2026-08-14/26)
#   per SEO-aggregator coverage (llm-stats.com, explainx.ai) — checked the
#   NVIDIA NIM catalog directly (primary source, GET /v1/models) for both:
#   neither is listed, so no free hosting confirmed yet. Not added; recheck
#   NIM in a future audit. No new free cloud providers found (aggregator
#   results for Cloudflare Workers AI / Mistral free tier / SambaNova were
#   blog-sourced, not primary — not evaluated further this pass). No local
#   (Ollama/Apple Silicon 16GB) upgrade search performed this pass.
# NOTE 2026-08-30 (model-scout daily audit): live probes (direct chat-completion
#   calls, not just listing endpoints) against every model referenced in this
#   file. Found ONE issue: minimaxai/minimax-m3 on NVIDIA NIM hangs
#   indefinitely — connects fine, no response ever arrives, confirmed across 3
#   separate curl probes (--max-time 20-30s, exit code 28/timeout, HTTP 000).
#   Same failure mode as meta/muse-glimmer-30b, removed for the identical
#   reason 2026-08-29. Removed from both categories it was in (summarization,
#   context); moonshotai/kimi-k3 and deepseek-ai/deepseek-v4-flash-0731 still
#   cover those slots on NVIDIA. No code changes needed this pass — router/
#   registry.py and dispatcher.py never referenced minimax-m3 by name.
#   Everything else live-checked and confirmed still fine: deepseek-ai/
#   deepseek-v4-flash-0731 and moonshotai/kimi-k3 (both NVIDIA NIM, 200 OK);
#   openai/gpt-oss-{20b,120b} and qwen/qwen3.6-27b (all Groq, 200 OK);
#   gemini-3.1-flash-lite and gemini-3.5-flash (both Gemini, 200 OK);
#   nvidia/nemotron-3.5-lightning:free, nvidia/nemotron-3-nano-omni-30b-a3b-
#   reasoning:free, nvidia/nemotron-3-{super-120b-a12b,ultra-550b-a55b}:free,
#   poolside/laguna-s-2.1:free, cohere/north-mini-code:free (all OpenRouter,
#   200 OK); google/gemma-4-{31b,26b-a4b}-it:free and z-ai/glm-5.2:free (all
#   OpenRouter, 429 "temporarily rate-limited upstream, shared pool" — same
#   recurring congestion pattern as every prior audit, not EOL, kept per
#   policy). Re-scanned the live OpenRouter free catalog (GET
#   /v1/api/v1/models, filtered ':free'): unchanged, same 18 entries as the
#   2026-08-28/29 scans. Web search for new open-weight releases and free
#   providers surfaced nothing dated/primary beyond what's already noted;
#   specifically checked whether Qwen3.8-2.4T-A95B (surfaced 2026-08-29) is
#   reachable via NVIDIA's free hosted API — confirmed NOT: docs.nvidia.com's
#   own get-started guide for it describes self-hosted Kubernetes NIM
#   container deployment only (4-node minimum, Helm/kubectl), not
#   integrate.api.nvidia.com; also absent from the live GET /v1/models
#   listing. GLM-5.3 likewise absent from that listing. No new free cloud
#   providers found (search results were the same recycled SEO-blog
#   aggregator noise as prior audits — laozhang.ai, pasqualepillitteri.it,
#   etc. — nothing dated+primary). Local (Ollama/Apple Silicon 16GB) check:
#   `ollama` CLI not available in this session's environment, no live
#   inventory check possible; web search surfaced an unverified claim
#   (aggregator-sourced, no primary citation) that Qwen3.8-27B released
#   ~2026-08-14 with fast community GGUF adoption — worth a follow-up check
#   against `ollama list`/`ollama pull` from a session where the CLI is
#   reachable, not acted on here.
# NOTE 2026-08-31 (model-scout daily audit): live probes (direct chat-
#   completion calls) against every model referenced in this file. All still
#   live, no removals: deepseek-ai/deepseek-v4-flash-0731 and moonshotai/
#   kimi-k3 (both NVIDIA NIM, 200 OK — deepseek-v4-flash-0731 needed a 30s
#   timeout to clear a transient 529 "Service temporarily overloaded", second
#   attempt succeeded); openai/gpt-oss-{20b,120b} and qwen/qwen3.6-27b (all
#   Groq, 200 OK); gemini-3.1-flash-lite and gemini-3.5-flash (both Gemini,
#   200 OK); cohere/north-mini-code:free, nvidia/nemotron-3-{super-120b-a12b,
#   ultra-550b-a55b}:free, poolside/laguna-s-2.1:free (all OpenRouter, 200
#   OK); google/gemma-4-31b-it:free and z-ai/glm-5.2:free (OpenRouter, 429
#   "temporarily rate-limited upstream, shared pool" on two separate attempts
#   ~10s apart — same recurring congestion as every prior audit, not EOL,
#   kept per policy). Re-scanned live OpenRouter free catalog (GET /v1/models,
#   filtered ':free'): 18 entries, unchanged from 2026-08-28/29/30 scans.
#   Re-scanned live NVIDIA NIM catalog (GET /v1/models): 83 entries; both
#   referenced models present; meta/muse-glimmer-30b and minimaxai/minimax-m3
#   still listed there too but correctly stay unreferenced (removed
#   2026-08-29/30 for hanging, not for disappearing from the catalog — NIM
#   doesn't delist hung models, it just never responds). Web search for new
#   open-weight releases and free providers: nothing dated+primary — same
#   recycled SEO-blog aggregator noise (taskade.com, morphllm.com, costbench.
#   com, freellmapi.co, etc.) reporting on models already in this cascade
#   (Kimi K3, GLM-5.2, MiniMax M3, Qwen3.6) or repackaging stale claims
#   (Cerebras/SambaNova free-tier status conflicting across sources, neither
#   confirmed via a primary page). Local (Ollama/Apple Silicon 16GB) check:
#   `ollama` CLI still not available in this session's environment, no live
#   inventory check possible. Found one primary source worth noting for
#   context, not action: ollama.com/blog/mlx confirms Ollama 0.19's MLX
#   backend (up to ~2x decode speed on Apple Silicon) requires 32GB+ unified
#   memory — this machine's 16GB stays on the existing Metal/llama.cpp
#   backend, no change applicable here.

# NOTE 2026-09-01 (model-scout daily audit): live probes (direct chat-
#   completion calls) against every model referenced in this file. All still
#   live, no removals: deepseek-ai/deepseek-v4-flash-0731 and moonshotai/
#   kimi-k3 (both NVIDIA NIM, 200 OK); openai/gpt-oss-{20b,120b} and
#   qwen/qwen3.6-27b (all Groq, 200 OK); gemini-3.1-flash-lite and
#   gemini-3.5-flash (both Gemini, 200 OK); nvidia/nemotron-3.5-lightning:free,
#   nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free, cohere/north-mini-
#   code:free, nvidia/nemotron-3-ultra-550b-a55b:free (all OpenRouter, 200 OK
#   — ultra-550b-a55b needed ~30s to respond, slow but not dead); google/
#   gemma-4-{31b,26b-a4b}-it:free, z-ai/glm-5.2:free, poolside/laguna-s-2.1:free
#   (all OpenRouter, 429 "temporarily rate-limited upstream, shared pool" —
#   same recurring congestion as every prior audit, not EOL, kept per policy);
#   nvidia/nemotron-3-super-120b-a12b:free (OpenRouter, 502 "Upstream error
#   from Nvidia: Service temporarily overloaded" on 3 separate attempts ~10s
#   apart — treated as transient overload per the same policy applied to 429s,
#   not removed, recheck next audit if it persists). ADDED: qwen/qwen3.8-27b
#   to Groq/`instruction` — new on Groq's catalog since the 2026-08-31 audit
#   (Alibaba, released 2026-08-14, Apache 2.0, huggingface.co/Qwen/Qwen3.8-27B,
#   Artificial Analysis Intelligence Index 52 / SWE-bench Pro 61.7), verified
#   non-reasoning via two live probes (no <think>/reasoning_content, no
#   length-cutoff-on-empty-content at max_tokens=5) — closes the Groq gap in
#   `instruction` open since llama-4-scout's EOL on 2026-08-24. Also added to
#   benchmark/providers/groq_provider.py FREE_MODELS. Re-scanned live
#   OpenRouter free catalog (GET /v1/models, filtered ':free'): 17 entries,
#   unchanged from 2026-08-28 through 08-31 scans (still includes the
#   already-evaluated-and-rejected inclusionai/ling-3.0-flash-fin:free,
#   thinkingmachines/inkling(-small):free, dots-studio/dots-3-note-
#   preview:free, liquid/lfm-2.5-2.6b:free, poolside/laguna-xs-2.1:free,
#   minimax/minimax-{m2.7,m3}:free, nvidia/nemotron-3.5-content-safety:free).
#   Re-scanned live NVIDIA NIM catalog (GET /v1/models): 83 entries, same
#   count as 2026-08-31, no new listings. Web search for new open-weight
#   releases and free providers beyond the Groq catalog diff above surfaced
#   nothing dated+primary — same recycled SEO-aggregator noise as prior
#   audits (thundercompute.com, bentoml.com, llm-stats.com, etc.) covering
#   models already in this cascade. Local (Ollama/Apple Silicon 16GB) check:
#   `ollama` CLI still not available in this session's environment, no live
#   inventory check possible.
# NOTE 2026-09-02 (model-scout daily audit): live probes (direct chat-
#   completion calls) against every model referenced in this file. Found ONE
#   EOL: kwaipilot/kat-coder-air-v2.5 on OpenRouter — HTTP 404 "No endpoints
#   found", confirmed via GET /v1/models too (slug no longer listed; only
#   kwaipilot/kat-coder-pro-v2 and kat-coder-pro-v2.5 remain). This was the
#   sole `cheapest:coding`/`cheapest:code_debug` paid fallback rung — removed
#   from both categories, no replacement. Tried the two surviving Kat-Coder
#   Pro variants as a replacement but both returned HTTP 403 "Key limit
#   exceeded (total limit)" on our OPENROUTER_API_KEY — this is an
#   account-level spend cap, not a model issue, and it blocks ANY paid
#   OpenRouter call on this key right now, not just Kat-Coder. Flagged to
#   the user rather than swapped in blind: raising/resetting the OpenRouter
#   key's spend limit is an account decision, and a paid fallback nobody can
#   actually reach isn't worth adding back. `coding`/`code_debug` now have NO
#   `cheapest:*` paid rung — free-tier entries in both categories are
#   unaffected and still cover `best:`/`fastest:`.
#   Everything else live-checked and confirmed still fine: deepseek-ai/
#   deepseek-v4-flash-0731 (NVIDIA NIM, 529 "Service temporarily overloaded"
#   on first probe, 200 OK on retry — transient, not EOL) and moonshotai/
#   kimi-k3 (NVIDIA NIM, 200 OK); openai/gpt-oss-{20b,120b}, qwen/qwen3.6-27b,
#   qwen/qwen3.8-27b (all Groq, 200 OK — one initial network timeout on
#   gpt-oss-20b in this session's sandbox, cleared on retry, not an API
#   issue); gemini-3.1-flash-lite and gemini-3.5-flash (both Gemini, 200 OK);
#   nvidia/nemotron-3.5-lightning:free, cohere/north-mini-code:free, nvidia/
#   nemotron-3-ultra-550b-a55b:free (all OpenRouter, 200 OK); google/gemma-4-
#   {31b,26b-a4b}-it:free, z-ai/glm-5.2:free, poolside/laguna-s-2.1:free (all
#   OpenRouter, 429 "temporarily rate-limited upstream" — same recurring
#   congestion pattern as every prior audit, not EOL, kept per policy);
#   nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free and nvidia/nemotron-3-
#   super-120b-a12b:free (both OpenRouter, transient 502/ResourceExhausted
#   from the underlying Nvidia backend on first probe, one cleared to 200 on
#   retry — treated as congestion, not EOL, per the same policy applied to
#   429s in prior audits).
#   Re-scanned live OpenRouter free catalog (GET /v1/models, filtered
#   ':free'): 18 entries, unchanged from the 2026-09-01 scan (same set,
#   including nvidia/nemotron-3.5-content-safety:free noted then — a
#   moderation/safety classifier, off-category for every slot here, still
#   not added). Re-scanned live NVIDIA NIM catalog (GET /v1/models): 82
#   entries (down 1 from the 83 counted 2026-08-31/09-01, but every model
#   this file actually references is still present — no functional change,
#   NIM's total count fluctuates with unrelated embedding/reward/guard
#   models we don't use). Checked GLM-5.3 / GLM-5.3-Flash (released
#   2026-08-26) directly against OpenRouter's own model page: still paid-only
#   ($0.075/$0.25 per Mtok, temporary 50%-off promo through 2026-09-09), no
#   free tier anywhere — confirms the 2026-08-31 expectation, not added.
#   Checked SambaNova Cloud's own pricing page (cloud.sambanova.ai/pricing)
#   per the still-open plans/new-cloud-providers.md candidate — it lists
#   only paid per-token pricing, no free-tier terms visible on that page.
#   Inconclusive (a free tier could be documented elsewhere on their site),
#   not upgraded from "unverified" to accepted or rejected; left as-is for a
#   future pass. No new free cloud providers or local (Ollama/Apple Silicon
#   16GB) upgrades found this pass.
# NOTE 2026-09-04 (model-scout daily audit): live probes (direct chat-
#   completion calls) against every model referenced in this file. ZERO EOLs,
#   no changes needed. NVIDIA NIM: moonshotai/kimi-k3 (200 OK) and deepseek-ai/
#   deepseek-v4-flash-0731 (first probe HTTP 000/timeout at 90s, 200 OK on a
#   150s retry — same recurring transient-overload pattern as prior audits,
#   not EOL). Groq: openai/gpt-oss-{20b,120b}, qwen/qwen3.6-27b, qwen/
#   qwen3.8-27b (all 200 OK). Gemini: gemini-3.1-flash-lite, gemini-3.5-flash
#   (both 200 OK). OpenRouter: cohere/north-mini-code:free, nvidia/nemotron-3-
#   super-120b-a12b:free, nvidia/nemotron-3.5-lightning:free (all 200 OK);
#   nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free and nvidia/nemotron-3-
#   ultra-550b-a55b:free (HTTP 200 wrapping an upstream 429/502 "ResourceExhausted"
#   / "Service temporarily overloaded" from Nvidia's OpenRouter-hosted backend);
#   google/gemma-4-{31b,26b-a4b}-it:free, z-ai/glm-5.2:free, poolside/
#   laguna-s-2.1:free (429 "temporarily rate-limited upstream, shared pool" —
#   same recurring congestion as every prior audit, not EOL, kept per policy).
#   Re-scanned live OpenRouter free catalog (GET /v1/models, filtered ':free'):
#   18 entries, unchanged from 2026-09-03. Re-scanned live NVIDIA NIM catalog
#   (GET /v1/models): 81 entries (down from 82 on 2026-09-02/09-03); did not
#   diff the full list — both NVIDIA models used in this cascade were
#   independently confirmed alive via direct chat-completion probe above, so
#   the count drop doesn't affect this file; flagged for a closer diff next
#   pass if it keeps shrinking.
#   Web search for new open-weight releases and free providers returned only
#   recycled SEO-blog aggregator coverage of models/providers already known
#   (Qwen3.8-27B, Kimi K3, GLM-5.2, Gemini/Groq/OpenRouter/NVIDIA NIM as the
#   "genuine free tier" set, Cloudflare Workers AI/Mistral/SambaNova/Cohere
#   free tiers mentioned again with no primary-source depth beyond what was
#   already inconclusive as of 2026-08-28/09-02) — nothing new dated+primary.
#   Local (Ollama/Apple Silicon 16GB): `ollama` CLI still not available in
#   this session's environment, no live inventory check possible (same as
#   every prior audit). No new free cloud providers or local upgrades found
#   this pass.
# NOTE 2026-09-03 (model-scout daily audit): live probes (direct chat-
#   completion calls) against every model referenced in this file. ZERO EOLs,
#   no changes needed. NVIDIA NIM: moonshotai/kimi-k3 (200 OK) and deepseek-ai/
#   deepseek-v4-flash-0731 (two initial requests hit HTTP 000/timeout at 40-60s,
#   200 OK on a 90s retry — same transient-overload pattern as 2026-08-31/09-02,
#   not EOL). Groq: openai/gpt-oss-{20b,120b}, qwen/qwen3.6-27b, qwen/qwen3.8-27b
#   (all 200 OK). Gemini: gemini-3.1-flash-lite, gemini-3.5-flash (both 200 OK).
#   OpenRouter: cohere/north-mini-code:free, nvidia/nemotron-3-nano-omni-30b-
#   a3b-reasoning:free, nvidia/nemotron-3-super-120b-a12b:free, nvidia/
#   nemotron-3.5-lightning:free, nvidia/nemotron-3-ultra-550b-a55b:free (200 OK
#   — ultra-550b-a55b needed ~100s this time, slower than the ~30s noted
#   2026-09-01 but still alive, not EOL); google/gemma-4-{31b,26b-a4b}-it:free,
#   z-ai/glm-5.2:free, poolside/laguna-s-2.1:free (429 "temporarily rate-limited
#   upstream, shared pool" — same recurring congestion as every prior audit,
#   not EOL, kept per policy).
#   Re-scanned live OpenRouter free catalog (GET /v1/models, filtered ':free'):
#   18 entries, unchanged from the 2026-09-02 scan (same set, including the
#   already-evaluated-and-rejected inclusionai/ling-3.0-flash-fin:free,
#   thinkingmachines/inkling(-small):free, dots-studio/dots-3-note-
#   preview:free, liquid/lfm-2.5-2.6b:free, poolside/laguna-xs-2.1:free,
#   minimax/minimax-{m2.7,m3}:free, nvidia/nemotron-3.5-content-safety:free).
#   Re-scanned live NVIDIA NIM catalog (GET /v1/models): 82 entries, same count
#   as 2026-09-02, no new listings.
#   Web search for new open-weight releases returned only recycled SEO-blog
#   aggregator coverage of models already in this cascade (GLM-5.3-Flash,
#   DeepSeek V4-Pro, Kimi K3, Qwen3.8-27B) — nothing new dated+primary.
#   Followed up on the still-open SambaNova candidate from 2026-09-02 (their
#   own blog, sambanova.ai/blog/sambanova-cloud-developer-tier-is-live): the
#   "free" offer is a one-time $5 credit that expires after 3 months, not an
#   ongoing no-card free tier like Groq/NVIDIA/OpenRouter/Gemini — doesn't
#   confirm the aggregator's "200K tokens/day free tier" claim, and the blog
#   doesn't state whether a payment method is required to start. Still
#   inconclusive on primary-source grounds; not accepted or rejected, left
#   for a future pass. No new free cloud providers or local (Ollama/Apple
#   Silicon 16GB) upgrades found this pass.
# NOTE 2026-09-05 (model-scout daily audit): live probes (direct chat-
#   completion calls) against every model referenced in this file. ZERO EOLs,
#   no changes needed. NVIDIA NIM: moonshotai/kimi-k3 and deepseek-ai/
#   deepseek-v4-flash-0731 (both 200 OK). Groq: openai/gpt-oss-{20b,120b},
#   qwen/qwen3.6-27b, qwen/qwen3.8-27b (all 200 OK). Gemini: gemini-3.1-
#   flash-lite, gemini-3.5-flash (both 200 OK). OpenRouter: nvidia/
#   nemotron-3-super-120b-a12b:free, poolside/laguna-s-2.1:free, cohere/
#   north-mini-code:free, nvidia/nemotron-3-ultra-550b-a55b:free (200 OK);
#   google/gemma-4-31b-it:free and z-ai/glm-5.2:free hit the same recurring
#   429 "temporarily rate-limited upstream, shared pool" seen in every prior
#   audit — not EOL, kept per policy.
#   Re-scanned live NVIDIA NIM catalog (GET /v1/models, 81 entries) and
#   OpenRouter free catalog (GET /v1/models, 19 entries): no new listings
#   beyond what's already evaluated in prior audits. Re-confirmed
#   moonshotai/kimi-k2.6 and nvidia/nemotron-nano-3-30b-a3b still 404
#   ("Not found for account") on NIM chat completions despite being listed —
#   still dead for this account, matches every prior finding since
#   2026-08-27/29. Re-checked deepseek-ai/deepseek-v4-pro-0813 (200 OK):
#   independent web coverage now puts it at Terminal-Bench 2.1 87.9 (vendor)
#   vs 78.7 (independent replication) — the vendor number would beat
#   deepseek-v4-flash-0731's 82.7 already in this cascade, but the
#   independently-verified number does not. Per policy of not picking a
#   number in silence when sources disagree, both are noted here; not
#   swapped in given the independent figure is the more conservative read
#   and flash-0731 is a known-stable presence in the cascade.
#   Web search for new open-weight releases and local (Ollama/Apple Silicon
#   16GB) upgrades returned only recycled SEO-blog aggregator coverage with
#   no new dated primary-source releases — discarded per policy. No new free
#   cloud providers found. No changes to any provider file, registry, or
#   ranking this pass.
# NOTE 2026-09-06 (model-scout daily audit): live probes (direct chat-
#   completion calls) against every model referenced in this file. ZERO EOLs,
#   no changes needed. NVIDIA NIM: moonshotai/kimi-k3 and deepseek-ai/
#   deepseek-v4-flash-0731 (both 200 OK). Groq: openai/gpt-oss-{20b,120b},
#   qwen/qwen3.6-27b, qwen/qwen3.8-27b (all 200 OK). Gemini: gemini-3.1-
#   flash-lite, gemini-3.5-flash (both 200 OK). OpenRouter: nvidia/nemotron-3-
#   super-120b-a12b:free, nvidia/nemotron-3.5-lightning:free, nvidia/
#   nemotron-3-ultra-550b-a55b:free, poolside/laguna-s-2.1:free, cohere/
#   north-mini-code:free (200 OK); nvidia/nemotron-3-nano-omni-30b-a3b-
#   reasoning:free (200 wrapping upstream 502 "ResourceExhausted: Worker
#   local total request limit reached" — transient, not EOL); google/gemma-4-
#   {31b,26b-a4b}-it:free and z-ai/glm-5.2:free (429 "temporarily rate-limited
#   upstream, shared pool" — same recurring congestion as every prior audit,
#   not EOL, kept per policy).
#   Re-scanned live OpenRouter free catalog (GET /v1/models, filtered
#   ':free'): 19 entries, one new since 2026-09-05: inclusionai/ling-3.0-
#   flash-sante:free. Probed directly — same failure mode as the already-
#   rejected ling-3.0-flash-fin:free (health-domain fine-tune of ling-3.0-
#   flash, emits a <think>-style reasoning trace before any answer, off-
#   category for every slot in this file). Not added. Re-scanned live NVIDIA
#   NIM catalog (GET /v1/models): 81 entries, same count as 2026-09-05;
#   checked explicitly for GLM-5.3, Qwen3.8-2.4T-A95B, and DeepSeek V4 Flash
#   Vision Exp (released 2026-08-21 per web search) — none present, only
#   deepseek-v4-flash-0731 and deepseek-v4-pro-0813 remain the live DeepSeek
#   builds. Nothing to add.
#   Web search for new open-weight releases returned only recycled SEO-blog
#   aggregator coverage of models already in this cascade (GLM-5.3, Qwen3.8,
#   DeepSeek V4 variants, Kimi K3) — nothing new dated+primary. Followed up
#   again on the still-open SambaNova candidate: cloud.sambanova.ai/plans
#   itself now shows "credit card" language on the page (previously
#   ambiguous), consistent with the community report surfaced 2026-09-02/03
#   that a payment method was added to the free tier ~4 weeks prior. Signal
#   is trending toward reject but still not a clean primary statement of
#   terms — left as inconclusive rather than flipping the verdict on a page
#   scrape alone; worth a dedicated check of SambaNova's actual signup flow
#   in a future pass to close this out one way or the other.
#   No new free cloud providers or local (Ollama/Apple Silicon 16GB) upgrades
#   found this pass — `ollama` CLI still not available in this session's
#   environment, no live inventory check possible (same as prior audits). No
#   changes to any provider file, registry, or ranking this pass.
# NOTE 2026-09-07: z-ai/glm-5.2:free reached EOL on OpenRouter — direct probe
#   returns 404 "This model is unavailable for free. The paid version is
#   available now - use this slug instead: z-ai/glm-5.2", and it's gone from
#   the live free catalog entirely (18 entries, no glm-5.2). Only live use was
#   the `reasoning` category slot; replaced with minimax/minimax-m3:free
#   (427B MoE, 23B active, GPQA Diamond 93.5 per benchlm.ai — verified live,
#   clean 200 with direct content, no truncation). Updated FREE_MODELS in
#   benchmark/providers/openrouter_provider.py to match. Note: minimax-m3 was
#   previously dropped from the NVIDIA NIM context-category slot for hanging
#   indefinitely — that's NIM-specific and did not reproduce on OpenRouter.
#   All other live entries re-probed clean: NVIDIA NIM (kimi-k3,
#   deepseek-v4-flash-0731, deepseek-v4-pro-0813 — the latter back alive but
#   not needed, flash-0731 still benchmarks higher); Groq (openai/gpt-oss-
#   {20b,120b}, qwen/qwen3.6-27b, qwen/qwen3.8-27b, all 200 OK); Gemini
#   (gemini-3.5-flash 200 OK; gemini-3.1-flash-lite 503 "high demand" —
#   transient, confirmed still listed in /v1beta/models, not EOL).
#   Noted for a future pass, not acted on this time: Gemini released a third
#   Flash-tier model in six weeks — gemini-3.8-flash (2026-09-02), plus
#   3.6-flash/3.7-flash/3.5-flash-lite all now appear in the live models
#   list alongside the two already in this cascade. Free-tier RPM/RPD for
#   3.8-flash isn't published (only visible inside AI Studio per
#   eesel.ai/blog/gemini-3-8-flash), so not swapping blindly — worth
#   confirming actual free quota before promoting it over gemini-3.5-flash /
#   gemini-3.1-flash-lite.

categories:

  reasoning:
    # ── NVIDIA NIM ─────────────────────────────────────────────────────────
    # NOTE: z-ai/glm-5.1 reached EOL 2026-07-02, removed.
    - provider: nvidia
      model: deepseek-ai/deepseek-v4-flash-0731
      terminal_bench_2_1: 82.7
      context_window: 1000000
      note: "Replaces deepseek-ai/deepseek-v4-pro, EOL 2026-08-07 on NVIDIA NIM (410 'reached end of life'). Same successor model already used in coding/code_debug/context since 2026-08-22."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-24"
    - provider: nvidia
      model: moonshotai/kimi-k3
      note: "Replaces moonshotai/kimi-k2.6, which now 404s on NVIDIA NIM chat completions ('Not found for account') despite still being listed in /v1/models — treated as dead for this account. kimi-k3 already live and used in coding/code_debug/context since 2026-08-22."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-27"
    # NOTE: meta/muse-glimmer-30b removed 2026-08-29 — hangs indefinitely on
    #   NVIDIA NIM (connects, never returns; confirmed across 3 probes up to
    #   240s max-time). Not a clean EOL/404, but functionally worse for the
    #   cascade. deepseek-v4-flash-0731/kimi-k3 above still cover this category.
    # ── Groq ───────────────────────────────────────────────────────────────
    - provider: groq
      model: openai/gpt-oss-120b
      note: "Matches o4-mini on many benchmarks. 500 tok/s."
      source: "Groq deprecation notice 2026-06-29"
    - provider: groq
      model: qwen/qwen3.6-27b
      gpqa_diamond: 87.8
      source: "console.groq.com — 2026-06"
    - provider: groq
      model: openai/gpt-oss-20b
      note: "Replaces llama-3.1-8b-instant, deprecated by Groq 2026-08-16; gpt-oss-20b is Groq's official migration target."
      source: "console.groq.com/docs/deprecations — verified 2026-08-12"
    # ── OpenRouter (free tier) ─────────────────────────────────────────────
    - provider: openrouter
      model: nvidia/nemotron-3.5-lightning:free
      context_window: 131072
      note: "Replaces inclusionai/ling-3.0-flash:free — that promo ended 2026-08-03 as this file warned, confirmed gone from the live free catalog 2026-08-12. Lightning is new 2026-08-11, 30B MoE/3B active, ~4x faster output and 30% faster agentic completion vs class peers per NVIDIA's PinchBench."
      source: "openrouter.ai/api/v1/models — live-verified 2026-08-12; blogs.nvidia.com/blog/nemotron-lightning-switchyard-rtx-dgx"
    - provider: openrouter
      model: google/gemma-4-31b-it:free
      arena_elo: 1451
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    - provider: openrouter
      model: google/gemma-4-26b-a4b-it:free
      arena_elo: 1438
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    - provider: openrouter
      model: nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free
      context_window: 256000
      note: "Reasoning-tagged Nemotron variant. Replaces qwen3-next-80b-a3b-instruct:free (paid-only since ~2026-07)."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"
    - provider: openrouter
      model: minimax/minimax-m3:free
      gpqa_diamond: 93.5
      context_window: 1000000
      note: "Replaces z-ai/glm-5.2:free — confirmed dead 2026-09-07 (404 'unavailable for free, use paid z-ai/glm-5.2 instead'), gone entirely from the live OpenRouter free catalog. minimax-m3 (427B MoE, 23B active) live-probed clean 200 with direct content, no reasoning-token truncation. Note: minimax-m3 was separately removed from NVIDIA NIM (this file, context category) for hanging indefinitely — that issue is provider-specific to NIM, not reproduced on OpenRouter."
      source: "own live probe against openrouter.ai/api/v1/chat/completions — verified 2026-09-07; benchlm.ai/models/minimax-m3 (GPQA Diamond 93.5)"
    - provider: openrouter
      model: nvidia/nemotron-3-super-120b-a12b:free
      context_window: 1000000
      note: "Replaces meta-llama/llama-3.3-70b-instruct:free, which went paid-only."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"

  coding:
    # ── NVIDIA NIM ─────────────────────────────────────────────────────────
    - provider: nvidia
      model: moonshotai/kimi-k3
      context_window: 1000000
      note: "Replaces z-ai/glm-5.2 (EOL 2026-08-21 on NVIDIA NIM). 2.8T MoE, #3 Artificial Analysis (2026-07-16), free on NIM since weights opened 2026-07-26 — not yet in NIM's public docs, verified by direct probe."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-22"
    - provider: nvidia
      model: deepseek-ai/deepseek-v4-flash-0731
      terminal_bench_2_1: 82.7
      context_window: 1000000
      note: "13B active, 1M context. Beats deepseek-v4-pro (72.1) on Terminal-Bench 2.1 and all 9 of DeepSeek's published agent/coding benchmarks despite far fewer active params. Replaces deepseek-v4-pro in this category. Model id fixed 2026-08-22: unsuffixed deepseek-ai/deepseek-v4-flash hit EOL 2026-08-07; the dated build id (-0731) is the one still live."
      source: "techtimes.com/articles/322513; id fix verified live 2026-08-22"
    # NOTE: meta/muse-glimmer-30b removed 2026-08-29 — hangs indefinitely on
    #   NVIDIA NIM (see reasoning category note above for details).
    # ── Groq ───────────────────────────────────────────────────────────────
    - provider: groq
      model: qwen/qwen3.6-27b
      livecodebench_v6: 83.9
      swe_bench_verified: 77.2
      source: "console.groq.com — 2026-06"
    - provider: groq
      model: openai/gpt-oss-120b
      source: "Groq deprecation notice 2026-06-29"
    - provider: groq
      model: openai/gpt-oss-20b
      note: "Replaces llama-3.1-8b-instant, deprecated by Groq 2026-08-16; gpt-oss-20b is Groq's official migration target."
      source: "console.groq.com/docs/deprecations — verified 2026-08-12"
    # ── OpenRouter (free tier) ─────────────────────────────────────────────
    - provider: openrouter
      model: poolside/laguna-s-2.1:free
      swe_bench_multilingual: 78.5
      terminal_bench_2_1: 70.2
      context_window: 1000000
      note: "New 2026-07-21. 118B MoE, 8B active, thinking mode on. Beats models 10x its size on agentic coding; best OpenRouter free-tier coding option found so far. Use thinking mode — non-thinking drops Terminal-Bench to 60.4."
      source: "openrouter.ai/api/v1/models — verified 2026-07-23"
    - provider: openrouter
      model: cohere/north-mini-code:free
      context_window: 256000
      note: "Coding-specialized Cohere model. Replaces qwen3-next-80b-a3b-instruct:free (paid-only since ~2026-07). Uses hidden reasoning tokens before the answer — give it enough max_tokens headroom or it hits finish_reason=length with empty content."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"
    - provider: openrouter
      model: google/gemma-4-31b-it:free
      arena_elo: 1451
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    - provider: openrouter
      model: nvidia/nemotron-3-super-120b-a12b:free
      context_window: 1000000
      note: "Replaces meta-llama/llama-3.3-70b-instruct:free, which went paid-only."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"

  math:
    # ── NVIDIA NIM ─────────────────────────────────────────────────────────
    - provider: nvidia
      model: deepseek-ai/deepseek-v4-flash-0731
      terminal_bench_2_1: 82.7
      context_window: 1000000
      note: "Replaces deepseek-ai/deepseek-v4-pro, EOL 2026-08-07 on NVIDIA NIM (410 'reached end of life'). Same successor model already used in coding/code_debug/context."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-24"
    - provider: nvidia
      model: moonshotai/kimi-k3
      note: "Replaces moonshotai/kimi-k2.6, which now 404s on NVIDIA NIM chat completions ('Not found for account') despite still being listed in /v1/models — treated as dead for this account."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-27"
    # ── Groq ───────────────────────────────────────────────────────────────
    - provider: groq
      model: qwen/qwen3.6-27b
      aime_2026: 94.1
      note: "94.1% AIME 2026 — exceptional math."
      source: "console.groq.com — 2026-06"
    - provider: groq
      model: openai/gpt-oss-120b
      source: "Groq deprecation notice 2026-06-29"
    - provider: groq
      model: openai/gpt-oss-20b
      note: "Replaces llama-3.1-8b-instant, deprecated by Groq 2026-08-16; gpt-oss-20b is Groq's official migration target."
      source: "console.groq.com/docs/deprecations — verified 2026-08-12"
    # ── OpenRouter (free tier) ─────────────────────────────────────────────
    # NOTE: nvidia/nemotron-3-nano-30b-a3b:free removed 2026-08-27 — 404
    #   "unavailable for free" (paid-only now). No replacement needed, two
    #   other live free entries remain below.
    - provider: openrouter
      model: google/gemma-4-31b-it:free
      arena_elo: 1451
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    - provider: openrouter
      model: nvidia/nemotron-3-super-120b-a12b:free
      context_window: 1000000
      note: "Replaces meta-llama/llama-3.3-70b-instruct:free, which went paid-only."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"

  summarization:
    # ── NVIDIA NIM ─────────────────────────────────────────────────────────
    # NOTE: z-ai/glm-5.1 reached EOL 2026-07-02, removed.
    - provider: nvidia
      model: moonshotai/kimi-k3
      note: "Replaces moonshotai/kimi-k2.6, which now 404s on NVIDIA NIM chat completions ('Not found for account') despite still being listed in /v1/models — treated as dead for this account."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-27"
    # NOTE: minimaxai/minimax-m3 removed 2026-08-30 — hangs indefinitely on
    #   NVIDIA NIM (connects, never returns; 3 separate timeouts, same failure
    #   mode as meta/muse-glimmer-30b removed 2026-08-29). moonshotai/kimi-k3
    #   above still covers this category on NVIDIA.
    # ── Groq ───────────────────────────────────────────────────────────────
    - provider: groq
      model: openai/gpt-oss-120b
      source: "Groq deprecation notice 2026-06-29"
    - provider: groq
      model: qwen/qwen3.6-27b
      source: "console.groq.com — 2026-06"
    - provider: groq
      model: openai/gpt-oss-20b
      note: "Replaces llama-3.1-8b-instant, deprecated by Groq 2026-08-16; gpt-oss-20b is Groq's official migration target."
      source: "console.groq.com/docs/deprecations — verified 2026-08-12"
    # ── OpenRouter (free tier) ─────────────────────────────────────────────
    - provider: openrouter
      model: google/gemma-4-31b-it:free
      arena_elo: 1451
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    # NOTE: nvidia/nemotron-3-nano-30b-a3b:free removed 2026-08-27 — 404
    #   "unavailable for free" (paid-only now). No replacement needed, two
    #   other live free entries remain (one above, one below).
    - provider: openrouter
      model: nvidia/nemotron-3-super-120b-a12b:free
      context_window: 1000000
      note: "Replaces meta-llama/llama-3.3-70b-instruct:free, which went paid-only."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"

  instruction:
    # Reasoning models (qwen3.6-27b, kimi-k2.6, gpt-oss-120b) excluded:
    # their <think> blocks consume the entire token budget on low max_tokens
    # requests (e.g. single-word classifiers). Router now strips <think> blocks
    # before the empty-content check, but best to keep reasoning models out of
    # this category altogether. Use best:reasoning for tasks that want CoT.
    # NOTE: thinkingmachines/inkling reached EOL on NVIDIA NIM 2026-08-25
    #   (410 "reached its end of life"), removed 2026-08-27, no replacement.
    # NOTE: meta/muse-glimmer-30b removed 2026-08-29 — hangs indefinitely on
    #   NVIDIA NIM (connects, never returns, confirmed across 3 probes up to
    #   240s max-time). It was this category's ONLY NVIDIA entry and, worse,
    #   the FIRST entry tried under `best:instruction` — every call would
    #   have eaten a full 120s NVIDIA timeout before falling back. GAP: no
    #   NVIDIA entry remains in this category; gemma-4-31b-it:free
    #   (OpenRouter) and Gemini below still cover it. Recheck in a future
    #   audit for a non-reasoning NVIDIA replacement.
    # NOTE: llama/llama-4-scout-17b-16e-instruct reached EOL on Groq (404,
    #   fully gone from the catalog, not just renamed) — removed 2026-08-24.
    #   Groq gap CLOSED 2026-09-01: qwen/qwen3.8-27b appeared on Groq's
    #   catalog (new, not present as of the 2026-08-31 audit) and verified
    #   non-reasoning — see entry and note below.
    # ── Groq ───────────────────────────────────────────────────────────────
    - provider: groq
      model: qwen/qwen3.8-27b
      note: "Fills the Groq gap in this category, open since llama-4-scout-17b-16e-instruct's EOL on 2026-08-24. New model on Groq's catalog as of this audit (not present 2026-08-31), successor to qwen3.6-27b. Verified non-reasoning: two live probes (plain prompt, and a 5-max_tokens classifier-style prompt) returned direct content with no <think> block or reasoning_content field and no length-cutoff-on-empty-content — unlike qwen3.6-27b, which does emit reasoning traces and stays excluded from this category by the rule above."
      source: "own live probe against api.groq.com/openai/v1/chat/completions — verified 2026-09-01; huggingface.co/Qwen/Qwen3.8-27B (released 2026-08-14, Apache 2.0), artificialanalysis.ai/models/qwen3-8-27b (Intelligence Index 52, SWE-bench Pro 61.7)"
    # ── OpenRouter (free tier) ─────────────────────────────────────────────
    # NOTE: inclusionai/ling-3.0-flash:free removed — TIME-LIMITED PROMO ended
    #   2026-08-03 as this file warned, confirmed gone from live free catalog
    #   2026-08-12. Not replaced with nemotron-3.5-lightning:free (added to
    #   `reasoning`) since its reasoning-trace behavior isn't yet verified and
    #   this category deliberately excludes anything with a <think> block.
    - provider: openrouter
      model: google/gemma-4-31b-it:free
      arena_elo: 1451
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    # NOTE: thinkingmachines/inkling:free removed 2026-08-27 — now 403s
    #   ("only available on agentic harnesses"), not usable as a plain
    #   chat-completion model. z-ai/glm-5.2:free (used as its replacement in
    #   `reasoning`) was deliberately NOT reused here: its <think>-block
    #   behavior isn't verified and this category excludes anything with a
    #   reasoning trace. No replacement added — gemma-4-31b-it:free above and
    #   Gemini below still cover this category.
    # ── Gemini (independent 4th free provider, added 2026-08-24) ───────────
    - provider: gemini
      model: gemini-3.1-flash-lite
      note: "Fills the gap noted above — Groq has no non-reasoning instruct model free right now. Flash-Lite is not a CoT/reasoning model, safe for low-max_tokens instruction-following. Independent 4th provider (own quota/infra) for cascade resilience after the 2026-08-24 incident where Groq/NVIDIA/OpenRouter all failed at once. No card required."
      source: "ai.google.dev/gemini-api/docs/openai, aifreeapi.com free-tier guide — verified 2026-08-24"

  multilingual:
    # Speed-first ordering (Luganense conversational latency, 2026-07-16):
    # Groq LPU answers in 2-8s vs 40-60s for NVIDIA 397B. Quality fallback retained.
    # Trimmed from 9 entries to 4 — long tail was accumulating timeout penalties.
    # ── Groq (fast LPU) ────────────────────────────────────────────────────
    - provider: groq
      model: openai/gpt-oss-20b
      note: "Replaces llama-3.1-8b-instant (deprecated by Groq 2026-08-16). Primary: Groq LPU, still sub-10s typical. Sufficient for conversational multilingual."
      source: "console.groq.com/docs/deprecations — verified 2026-08-12"
    - provider: groq
      model: qwen/qwen3.6-27b
      note: "Balanced quality/speed on Groq LPU."
      source: "console.groq.com — 2026-06"
    # ── NVIDIA NIM (quality fallback) ──────────────────────────────────────
    - provider: nvidia
      model: moonshotai/kimi-k3
      note: "Quality fallback. Replaces moonshotai/kimi-k2.6, which now 404s on NVIDIA NIM chat completions ('Not found for account') despite still being listed in /v1/models — treated as dead for this account."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-27"
    # ── OpenRouter (free tier last resort) ─────────────────────────────────
    - provider: openrouter
      model: google/gemma-4-31b-it:free
      arena_elo: 1451
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    # ── Gemini (independent 4th free provider, added 2026-08-24) ───────────
    - provider: gemini
      model: gemini-3.1-flash-lite
      note: "Added as a genuinely independent 4th free provider (own quota, own infra — Google, not NVIDIA/Meta/Groq-hosted) after an incident where Groq/NVIDIA/OpenRouter all failed at once for Luganense's classifier. No card required for the free tier. 15 RPM / 1,000 RPD — most generous free quota of any tier here. No public Arena/AA score yet for this exact snapshot; placed as last resort pending benchmark data."
      source: "ai.google.dev/gemini-api/docs/openai (OpenAI-compat endpoint), aifreeapi.com free-tier guide — verified 2026-08-24"

  code_debug:
    # ── NVIDIA NIM ─────────────────────────────────────────────────────────
    - provider: nvidia
      model: moonshotai/kimi-k3
      context_window: 1000000
      note: "Replaces z-ai/glm-5.2 (EOL 2026-08-21 on NVIDIA NIM). 2.8T MoE, #3 Artificial Analysis (2026-07-16), free on NIM since weights opened 2026-07-26 — not yet in NIM's public docs, verified by direct probe."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-22"
    - provider: nvidia
      model: deepseek-ai/deepseek-v4-flash-0731
      terminal_bench_2_1: 82.7
      context_window: 1000000
      note: "13B active, 1M context. Beats deepseek-v4-pro (72.1) on Terminal-Bench 2.1 and all 9 of DeepSeek's published agent/coding benchmarks despite far fewer active params. Replaces deepseek-v4-pro in this category. Model id fixed 2026-08-22: unsuffixed deepseek-ai/deepseek-v4-flash hit EOL 2026-08-07; the dated build id (-0731) is the one still live."
      source: "techtimes.com/articles/322513; id fix verified live 2026-08-22"
    # NOTE: meta/muse-glimmer-30b removed 2026-08-29 — hangs indefinitely on
    #   NVIDIA NIM (see reasoning category note above for details).
    # ── Groq ───────────────────────────────────────────────────────────────
    - provider: groq
      model: qwen/qwen3.6-27b
      livecodebench_v6: 83.9
      swe_bench_verified: 77.2
      source: "console.groq.com — 2026-06"
    - provider: groq
      model: openai/gpt-oss-120b
      source: "Groq deprecation notice 2026-06-29"
    - provider: groq
      model: openai/gpt-oss-20b
      note: "Replaces llama-3.1-8b-instant, deprecated by Groq 2026-08-16; gpt-oss-20b is Groq's official migration target."
      source: "console.groq.com/docs/deprecations — verified 2026-08-12"
    # ── OpenRouter (free tier) ─────────────────────────────────────────────
    - provider: openrouter
      model: poolside/laguna-s-2.1:free
      swe_bench_multilingual: 78.5
      terminal_bench_2_1: 70.2
      context_window: 1000000
      note: "New 2026-07-21. 118B MoE, 8B active, thinking mode on. Beats models 10x its size on agentic coding/debugging; best OpenRouter free-tier option found so far."
      source: "openrouter.ai/api/v1/models — verified 2026-07-23"
    - provider: openrouter
      model: cohere/north-mini-code:free
      context_window: 256000
      note: "Coding-specialized Cohere model. Replaces qwen3-next-80b-a3b-instruct:free (paid-only since ~2026-07). Uses hidden reasoning tokens before the answer — give it enough max_tokens headroom or it hits finish_reason=length with empty content."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"
    - provider: openrouter
      model: google/gemma-4-31b-it:free
      arena_elo: 1451
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    - provider: openrouter
      model: nvidia/nemotron-3-super-120b-a12b:free
      context_window: 1000000
      note: "Replaces meta-llama/llama-3.3-70b-instruct:free, which went paid-only."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"

  context:
    # ── NVIDIA NIM ─────────────────────────────────────────────────────────
    - provider: nvidia
      model: moonshotai/kimi-k3
      context_window: 1000000
      note: "1M token context. Replaces z-ai/glm-5.2 (EOL 2026-08-21 on NVIDIA NIM). 2.8T MoE, free on NIM since weights opened 2026-07-26 — not yet in NIM's public docs, verified by direct probe."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-22"
    # NOTE: minimaxai/minimax-m3 removed 2026-08-30 — hangs indefinitely on
    #   NVIDIA NIM (see summarization category note above for details).
    - provider: nvidia
      model: deepseek-ai/deepseek-v4-flash-0731
      terminal_bench_2_1: 82.7
      context_window: 1000000
      note: "Replaces deepseek-ai/deepseek-v4-pro, EOL 2026-08-07 on NVIDIA NIM (410 'reached end of life'). Same successor model already used in coding/code_debug/reasoning/math."
      source: "own live probe against integrate.api.nvidia.com/v1/chat/completions — verified 2026-08-24"
    # NOTE: minimaxai/minimax-m2.7 reached EOL 2026-07-27, removed (no replacement needed — two NVIDIA entries above already cover 1M context).
    # ── Groq ───────────────────────────────────────────────────────────────
    - provider: groq
      model: openai/gpt-oss-120b
      source: "Groq deprecation notice 2026-06-29"
    - provider: groq
      model: qwen/qwen3.6-27b
      source: "console.groq.com — 2026-06"
    - provider: groq
      model: openai/gpt-oss-20b
      note: "Replaces llama-3.1-8b-instant, deprecated by Groq 2026-08-16; gpt-oss-20b is Groq's official migration target."
      source: "console.groq.com/docs/deprecations — verified 2026-08-12"
    # ── OpenRouter (free tier) ─────────────────────────────────────────────
    - provider: openrouter
      model: nvidia/nemotron-3-ultra-550b-a55b:free
      context_window: 1000000
      note: "Largest model on OpenRouter's free tier (550B MoE); best-fit replacement for qwen3-next-80b-a3b-instruct:free (paid-only since ~2026-07) in this category."
      source: "openrouter.ai/api/v1/models — verified 2026-07-21"
    - provider: openrouter
      model: nvidia/nemotron-3-super-120b-a12b:free
      arena_elo: 1343
      context_window: 1000000
      note: "1M token context on OpenRouter free tier."
      source: "openrouter.ai/models — 2026-06"
    - provider: openrouter
      model: google/gemma-4-31b-it:free
      arena_elo: 1451
      context_window: 262144
      source: "lmmarketcap.com/benchmarks/arena_elo — 2026-06"
    # NOTE: nvidia/nemotron-nano-9b-v2:free removed 2026-08-27 — 404 "No
    #   endpoints found". No replacement needed, three other live free
    #   entries remain in this category (two above, Gemini below).
    # ── Gemini (independent 4th free provider, added 2026-08-24) ───────────
    - provider: gemini
      model: gemini-3.5-flash
      context_window: 1048576
      note: "1,048,576 token context (confirmed spec, not extrapolated). Used here over the flash-lite variant used in other categories because context work benefits more from Flash's extra quality; free tier is smaller (10 RPM / 250 RPD vs flash-lite's 15/1,000) so kept as a lower-priority fallback. Independent 4th provider for cascade resilience — no card required."
      source: "ai.google.dev/gemini-api/docs/whats-new-gemini-3.5, aifreeapi.com free-tier guide — verified 2026-08-24"
